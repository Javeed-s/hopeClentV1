import React from "react";
import { useSelector } from "react-redux";;
import { IAppState } from "../types/global-types";

export default function useLoginPrompt() {
  // const { isLogin } = useSelector((state: IAppState) => state.userReducer);
  // const { handleModal }: any = React.useContext(ModalContext);

  // const showLogin = () => {
  //   if (!isLogin) {
  //     return handleModal(<LoginSignUp />);
  //   }
  // };

  // return { showLogin };
}
